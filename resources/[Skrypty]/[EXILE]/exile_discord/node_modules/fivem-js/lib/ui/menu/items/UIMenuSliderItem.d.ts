import { BadgeStyle } from '../../../enums';
import { UIMenuItem } from './';
export declare class UIMenuSliderItem extends UIMenuItem {
    private arrowLeft;
    private arrowRight;
    private arrowOnlyOnSelected;
    private rectangleBackground;
    private rectangleSlider;
    private rectangleDivider;
    private items;
    private index;
    Index: number;
    constructor(text: string, items: any[], index: number, description?: string, divider?: boolean, arrowOnlyOnSelected?: boolean);
    setVerticalPosition(y: number): void;
    indexToItem(index: number): any;
    draw(): void;
    setRightBadge(badge: BadgeStyle): void;
    setRightLabel(text: string): void;
}
